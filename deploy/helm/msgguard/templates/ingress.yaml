{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}-ingress
  annotations:
    {{- if .Values.ingress.mtls.enabled }}
    {{ .Values.ingress.mtls.authSecret | default "nginx.org/mtls-ca-secret" }}: {{ .Values.ingress.mtls.secretName | quote }}
    nginx.org/mtls-auth-type: "client-cert"
    {{- end }}
    {{- if .Values.ingress.waf.enabled }}
    {{- range $k, $v := .Values.ingress.waf.annotations }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
    {{- end }}
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Chart.Name }}-gateway
                port:
                  number: {{ .Values.service.port }}
{{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-gateway
spec:
  selector:
    app: {{ .Chart.Name }}-gateway
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
